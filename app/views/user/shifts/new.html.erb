<h1>New shift</h1>

<%= render "form", url: user_user_shifts_path(user_id: current_user.id), method: :post%>

<br>

<div>
  <%= link_to "Back to shifts", user_user_shifts_path(current_user.id, @shifts) %>
</div>
